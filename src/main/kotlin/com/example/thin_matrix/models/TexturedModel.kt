package com.example.thin_matrix.models

import com.example.spookycopengl.graphic.Texture
import com.example.thin_matrix.textures.ModelTexture

class TexturedModel(
    val model: RawModel,
    val texture: Texture
) {
}